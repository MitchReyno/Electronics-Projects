# VCA

A voltage-controlled amplifier (VCA) Eurorack module.

## What it does

Controls the amplitude of an audio signal using a control voltage (CV) input.

## Files

KiCad 8 project files are in the `VCA/` subdirectory (`.kicad_sch`, `.kicad_pcb`, `.kicad_pro`).
